# Product import

Catalog imports map CSV/JSON columns to title, description, category, price, SKU, stock, images, and generic product or variant attributes. Import jobs are tracked through `catalog_import_jobs`.

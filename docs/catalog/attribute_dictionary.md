# Attribute dictionary

The Attribute Dictionary replaces the fashion-only dictionary. Shop aliases override system aliases, and category scoped aliases can normalize values like `۱۲۸ گیگ` to `128GB`, cosmetics shades, food weights, or clothing colors and sizes.
